@model OrdersViewModel
@{ 
    ViewBag.Title = "Orders";
}
<h1>Orders</h1>
@for(int i = 0; i < Model.Orders.Count; i++)
{
    <ul>
        <li>Name : @Model.Orders[i].User.FirstName</li>
        <li>Pizza: @Model.Orders[i].Pizzas[0].Name</li>
    </ul>
}