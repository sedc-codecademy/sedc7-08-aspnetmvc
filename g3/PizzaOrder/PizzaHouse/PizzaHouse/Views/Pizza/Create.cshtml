@model PizzaHouse.ViewModels.PizzaViewModel
@{
    ViewData["Title"] = "Create";
}

<h2>Create new pizza</h2>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create" asp-controller="Pizza">
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SelectedIngredients" class="control-label"></label>
                <select asp-for="SelectedIngredients" asp-items="Model.AllIngredients" class="form-control"></select>
                <span asp-validation-for="SelectedIngredients" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BasePrice" class="control-label"></label>
                <input asp-for="BasePrice" class="form-control" />
                <span asp-validation-for="BasePrice" class="text-danger"></span>
            </div>
            <input type="submit" value="Create" class="btn btn-primary" />
        </form>

        @*@using (Html.BeginForm("Create", "Pizza", FormMethod.Post))
            {
                @Html.ValidationSummary(true)
                <div class="form-group">
                    @Html.LabelFor(m => m.Name, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Name, string.Empty, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Description, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Description, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Description, string.Empty, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.SelectedIngredients, new { @class = "control-label" })
                    @Html.DropDownListFor(m=>m.SelectedIngredients, Model.AllIngredients, new { @class = "form-control", multiple = "multiple" })
                    @Html.ValidationMessageFor(m => m.SelectedIngredients, string.Empty, new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.BasePrice, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.BasePrice, new { @class = "form-control", type = "number" })
                    @Html.ValidationMessageFor(m => m.BasePrice, string.Empty, new { @class = "text-danger" })
                </div>
                <input type="submit" value="Create" class="btn btn-primary" />
            }*@
    </div>
</div>